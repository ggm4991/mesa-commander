import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.goncholoko.mesacommander',
  appName: 'Mesa Commander',
  webDir: 'dist'
};

export default config;
