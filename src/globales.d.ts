// Inyectadas por `vite.config.ts` (build real) y `vitest.config.ts` (tests):
// la versión de `package.json` y el instante del build, para poder distinguir
// una build de otra a simple vista (ver ADR 0036).
declare const __APP_VERSION__: string
declare const __BUILD_TIME__: string
